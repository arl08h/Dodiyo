import Typed from 'typed.js';

var typed = new Typed('#typed', {
  strings: ['Design', 'Optimize', 'Magic'],
  typeSpeed: 100,
  backSpeed: 100,
  backDelay: 800,
  startDelay: 1500,
  loop: false,
});
